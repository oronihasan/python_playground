#  python .\traspose.py     

lst = [
    [1, 2, 3, 10],       #rows
    [4, 5, 6, 11],
    [7, 8, 9, 12]
]

def transpose(lst):

    o = []

    lRl = len(lst[0])
    lCl = len(lst)
    
    
    for i in range(lCl):
        
        



transpose(lst)